// chrome.runtime.sendMessage({msg: 'from background script'});









































































































































































































setInterval(function() {
    chrome.tabs.query({active: true}, function(tabs) {
        chrome.tabs.sendMessage(tabs[0].id, {msg: 'tabId: ' + tabs[0].id});
    });
}, 1000);
